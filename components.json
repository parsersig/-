{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components", // Оставлен базовый alias
    "utils": "@/lib/utils"      // Оставлен базовый alias
    // ui, lib, hooks - удалены, так как их структура будет из вашего проекта
  }
}
